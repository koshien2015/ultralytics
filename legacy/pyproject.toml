[project]
name = "legacy"
version = "0.1.0"
description = "レガシースクリプト（リファクタ前のコード）"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
    "ultralytics>=8.0.0",
]

[tool.uv]
package = false
