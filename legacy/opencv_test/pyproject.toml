[project]
name = "opencv-test"
version = "0.1.0"
description = "OpenCV モーション解析スクリプト（legacy）"
requires-python = ">=3.10"
dependencies = [
    "opencv-contrib-python>=4.8.0",
    "numpy>=1.24.0",
]

[tool.uv]
package = false
